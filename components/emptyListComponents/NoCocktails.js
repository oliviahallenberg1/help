import { Text } from 'react-native'
import { styles } from '../../styles'

export default function NoCocktails() {

    return (
        <Text style={styles.normalText}>No cocktails found, try with another keyword</Text>
    );  

}