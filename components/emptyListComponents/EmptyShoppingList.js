import { Text } from 'react-native'
import { styles } from '../../styles'

export default function EmptyShoppingList() {

    return (
        <Text style={styles.normalText}>Shopping list is empty.</Text>
    );  

}